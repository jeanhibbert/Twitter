
http://rehansaeed.co.uk/reactive-extensions-part1-replacing-events/
- Complete Data layer implementation for persistance of Command History.
	- Complete EF blog post
- Implement Signal R implementation using Typescript
- Roll your own IOC container
- Each new client has it's own dedicated thread... is this really necessary? this is quite resource heavy.

- When client disconnects ensure closing of client is handled properly by server
Unable to read data from the transport connection: A blocking operation was interrupted by a call to WSACancelBlockingCall.
EndOfStream = 'this._reader.EndOfStream' threw an exception of type 'System.ObjectDisposedException'
